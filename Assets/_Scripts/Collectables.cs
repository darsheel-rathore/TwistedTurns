using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Collectables : MonoBehaviour
{
    /*
     * Task
     * 
     * 1. Make Gameobject set active/ mesh renderer to false on contact with player.
     * 2. Make gameobject change its position on contact with the player
     * 2. On trigger, game object calls GameManager and add +10 value to the score
     * 3. 
     * 
     */
}
