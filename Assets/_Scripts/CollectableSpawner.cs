using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollectableSpawner : MonoBehaviour
{
    /*
     * Task
     * 1. Spawn collectable on random locations
     * 2. To use random location, collect the locations of all the active platform and randomly select the platfrom and then place diamond on top of the platform
     */
}
